import algumacoisa from 'datocms-client'

export default async function recebedorDeRequests(request, response) {
    const TOKEN = 'e2b2e960b70d1d8c26bdd16218701c'
    response.json({
        dados:'algum dado qualquer'
    })
}